services:
  mc:
    restart: "no"
    image: ${IMAGE_TO_TEST:-energypatrikhu/pterodactyl-minecraft-server}
    environment:
      EULA: "TRUE"
      SETUP_ONLY: "TRUE"
      TYPE: QUILT
      VERSION: ${MINECRAFT_VERSION:-LATEST}
    volumes:
      - ./home/container:/home/container
